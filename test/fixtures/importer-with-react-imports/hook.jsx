import { useEffect, useCallback } from "react";
/**
 * @__IMPORTABLE_HOOK__
 */

export default function useTestHook() {
  useEffect(() => {});
  useCallback(() => {});
  const [a, b] = useState({})
  const yo = useRef(null)
}
