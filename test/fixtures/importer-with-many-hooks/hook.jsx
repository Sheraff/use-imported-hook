import { 
  useEffect,
  useCallback,
  useMemo,
  useLayoutEffect,
  useImperativeHandle,
} from 'react'

/**
 * @__IMPORTABLE_HOOK__
 */
export default function useTestHook() {
  useEffect(() => {})
  useCallback(() => {})
  useMemo(() => {})
  useLayoutEffect(() => {})
  useImperativeHandle(() => {})
}
